// Deprecated: Firebase initialization removed when migrating to Supabase.
// Keep this file as a no-op shim for a short transitional period.
// The real server-side client is now in `server/supabase.ts` and `server/storage.ts`.

const initialized = false;
const db = null;

export { initialized, db };
